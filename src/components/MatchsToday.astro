---
import { getMatchsToday } from '@services/matchstoday.js'
import { getAllLeagues } from '@services/leagues.js'
import MatchsTodayRow from '@components/MatchsTodayRow.astro'

const matchsToday = await getMatchsToday()
const allLeagues = await getAllLeagues()

---

{
  matchsToday
  .filter((matchs) => allLeagues.includes(matchs.competition))
  .map((matchs) => {
    return (
      <section class='my-2'>

          
        <h2 class=" font-bold  rounded-lg p-4 w-full text-left">{matchs.competition}</h2>
        
        <div class="content">
          
          <div class="swiper w-full h-full grid xl:grid-cols-2 snap-mandatory scroll-m-4">
            {matchs.matches
              .map((match) => (    

              <MatchsTodayRow
                local={match.teams[0]}
                visitant={match.teams[1]}
                hour={match.hour}
                date={match.formattedDate}
                hourMatch={match.hourMatch}
                round= {matchs.fecha}
                score={match.score}                
                />
                
            ))}
          </div>
        </div>
      </section>
    )
  })
}